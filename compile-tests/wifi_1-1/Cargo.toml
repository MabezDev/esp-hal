[package]
name    = "wifi_1-1"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
embassy-executor = "0.10.0"
embassy-net = { version = "0.9.1", features = [
    "dhcpv4",
    "medium-ethernet",
    "tcp",
    "dns",
] }
embassy-time = "0.5.0"
embedded-io-async = "0.7.0"
enumset = { version = "1.0.0", optional = true }
esp-alloc = { version = "0.10.0" }
esp-backtrace = { version = "0.19.0", features = [
    "panic-handler",
    "println",
] }
esp-bootloader-esp-idf = { version = "0.5.0" }
esp-hal = { version = "1.1.0" , features = ["log-04", "unstable"] }
esp-println = { version = "0.17.0", features = ["log-04"] }
esp-rtos = { version = "0.3.0", features = ["esp-radio", "embassy", "log-04"] }
esp-radio = { version = "0.18.0", features = [
    "log-04",
    "unstable",
    "wifi",
] }
reqwless = { version = "0.14.0", default-features = false }
static_cell = "2.1.0"

# Chip selection is derived from device metadata: the project builds for every chip
# whose `if` predicate holds, and the per-chip cargo feature is forwarded to each
# `chip-deps` crate. Adding a new chip needs no edit here.
[package.metadata.espressif]
compile-test = { if = 'wifi_driver_supported', chip-deps = ["esp-hal", "esp-rtos", "esp-radio", "esp-backtrace", "esp-bootloader-esp-idf"], features = ["esp-radio/wifi"], append = [ { if = 'wifi_driver_supported && bt_driver_supported', features = ["esp-radio/coex"] } ] }

[profile.release]
debug            = true
debug-assertions = true
lto              = "fat"
codegen-units    = 1
